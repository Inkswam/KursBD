<Window x:Class="HotelManagementSystem.ChangeRoomStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Зміна статусу номера" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Номер кімнати:" FontWeight="Bold"/>
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="RoomNumberTextBlock" VerticalAlignment="Center"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Поточний статус:" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="CurrentStatusTextBlock" VerticalAlignment="Center"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Новий статус:" FontWeight="Bold"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="StatusComboBox" Margin="0,5"/>
        
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Зберегти" Width="100" Margin="5,0" 
                    Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Скасувати" Width="100" Margin="5,0" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>